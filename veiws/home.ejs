<!DOCTYPE html>
<html lang="en">
<head>
    <style>
        /* Page background and font */
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #f0f4f8, #d9e2ec);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
        }

        /* Header styling */
        .header {
            font-size: 2.0rem;
            color: #2c3e50;
            margin-bottom: 30px;
            text-align: center;
        }

        /* Form container box */
        .form-container {
            background-color: #ffffff;
            padding: 30px 40px;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 450px;
        }

        /* Label styling */
        .label {
            display: block;
            font-size: 1.0rem;
            margin-bottom: 10px;
            color: #34495e;
            font-weight: 600;
        }

        /* Input field */
        .input {
            display: block;
            width: 100%;
            padding: 12px 15px;
            margin-bottom: 20px;
            border: 1px solid #ccd6e0;
            border-radius: 8px;
            font-size: 0.8rem;
            transition: border-color 0.3s;
        }

        .input:focus {
            border-color: #3498db;
            outline: none;
        }

        /* Button styling */
        .generate-button {
            display: block;
            width: 70%;
            padding: 12px 15px;
            background-color: #3498db;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-bottom: 15px;
        }

        .generate-button:hover {
            background-color: #2c80b4;
        }

        /* Short URL text */
        .short-link-text {
            font-size: 1rem;
            color: #2c3e50;
            background-color: #e6f0fa;
            padding: 12px 15px;
            border-radius: 8px;
            margin-top: 15px;
            text-align: center;
            word-break: break-all;
        }

        .short-link {
            color: #007bff;
            text-decoration: none;
            font-weight: 500;
            word-break: break-word;
        }

        .short-link:hover {
            text-decoration: underline;
        }


    </style>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <h1 class="header" >Mittor er URL Shortener 🚀</h1> 
    <div class="form-container" >
        <form method="post" action="/url">
            <label class="label">Enter your URL link</label>
            <input type="text" class="input" name="url" placeholder="https//:example.com" />
            <button class="generate-button">Generate Short ID</button> 
        </form>
        <% if(locals.id) { %>
        <p class="short-link-text">
            Short URL Generated:
            <a href="http://localhost:8001/url/<%= id %>" target="_blank" class="short-link">
                http://localhost:8001/url/<%= id %>
            </a>
        </p>
        <% } %> 
    </div>
</body>
</html>